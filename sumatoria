#include <stdio.h>
#include <stdlib.h>
float promedio (float a, float b) {
float a=5, b=10, prom;
prom = promedio (a,b);
printf("el promedio es : %2.1 f/n" prom);
system ("Pause");
resturn 0;
}
float promedio(float a, float b){
float prom;
a= a+3;
b= b+3;
prom= (a+b)/2;
return prom;
}
